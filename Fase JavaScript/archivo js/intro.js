// variables

var producto = 'Audifonos Gamer';

let producto = "micro";

function dividir(a, b) {

    if (b === 0) {
        throw new Error("&quot;No se puede dividir entre cero&quot");
    }
    return a / b;
}